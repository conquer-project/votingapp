on:
  push:
    branches:
      - main
    paths:
      - "result/**"
 
  pull_request:
    paths: 
      - "result/**"

jobs:

  call-docker-build:
    name: Call Docker Build
    uses: ./.github/workflows/reusable-docker-build.yaml@workflows

    permissions:
      id-token: write
      contents: read
      packages: write
      pull-requests: write
    
    with:
      role-to-assume: ${{ vars.GH_IDP_AWS_ROLE_TO_ASSUME }}
      context: ./result
      image-name: result